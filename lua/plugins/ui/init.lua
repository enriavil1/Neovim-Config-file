local colorscheme = require('plugins.ui.colorscheme')
local lualine = require('plugins.ui.lualine')
local alpha = require('plugins.ui.alpha')
local bufferline = require('plugins.ui.bufferline')
local noice = require('plugins.ui.noice')

return {
  colorscheme,
  lualine,
  alpha,
  bufferline,
  noice
}
